

sleep 30
openvswitch=`ifconfig | grep ovs_eth | wc -l`
if [ $openvswitch -eq "0" ]; then 
  interface=eth0
else
  interface=ovs_eth0
fi
iptables -t nat -A POSTROUTING -s 192.168.99.0/24 -o $interface -j MASQUERADE
